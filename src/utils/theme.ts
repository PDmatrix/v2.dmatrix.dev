import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  colors: {
    bg: '#282a36',
    textNormal: 'hsla(0, 0%, 100%, 0.88)',
    hr: 'hsla(0, 0%, 100%, 0.2)',
    codeBg: 'rgba(45, 45, 45, 0.44)',
    link: '#fffeaa',
  },
};

export default theme;
